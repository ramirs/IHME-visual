(function(){

	//full page splash - contact, credits, links to portfolio, etc.
	$('.page_info').click(function(){

	});

	//set up and maintain the context for slected regions
	$(".country_code").on('change', function(){
		// console.log($('#country_code')[0].value);
	});

})();